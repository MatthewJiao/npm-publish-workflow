export const areNumbersEqual = (a: number, b: number) => {
  return a === b;
};

export const getSum = (numbers: number[]): number => {
  return numbers.reduce((acc, num) => {
    return acc + num;
  }, 0);
};

export const getSum2 = (numbers: number[]): number => {
  return (
    1 +
    numbers.reduce((acc, num) => {
      return acc + num;
    }, 0)
  );
};

export const getSum3 = (numbers: number[]): number => {
  return numbers.reduce((acc, num) => {
    return acc + num;
  }, 0);
};

export const getSum4 = (numbers: number[]): number => {
  return numbers.reduce((acc, num) => {
    return acc + num;
  }, 0);
};

export const getSum5 = (numbers: number[]): number => {
  return numbers.reduce((acc, num) => {
    return acc + num;
  }, 0);
};
